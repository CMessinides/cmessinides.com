include ./summary

mixin writingSummary({ title, description, href, publication, date, variants, level = 3 } = {})
	- let classes = ['c-summary--writing']
	if variants
		- classes = classes.concat(variants.map(v => `c-summary--${v}`))
	+summary()(class=classes)
		+summaryLink(href)
			+summaryDetails
				+summaryDetail('Publication')= publication
				+summaryDetail('Date')= date
			+summaryHeading(level)= title
			+summaryDescription= description
